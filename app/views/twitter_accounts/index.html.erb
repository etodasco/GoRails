<h1>Twitter Accounts</h1>

<%= button_to "Connect a Twitter account", "/auth/twitter", data: { turbo: false }, local: true, method: :post, class: "btn btn-primary" %>

<% @twitter_accounts.each do |twitter_account| %>
    <div>
    <%= twitter_account.username %>
    </div>
<% end %>
